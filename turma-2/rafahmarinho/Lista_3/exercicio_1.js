let rs = require ('readline-sync')

let repeat = rs.questionInt (console.log('Digite um número de repetições de BIP:'))

let cont = 0

while (cont !== repeat ) {
    console.log ('BIP BIP')
    cont ++
    
}