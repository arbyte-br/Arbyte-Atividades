let readlineSync = require('readline-sync')
let numeroEntrada = readlineSync.questionInt('Digite um número de entrada ')

while (numeroEntrada > 0) {
    console.log('BIP BIP')
    numeroEntrada--
}
    