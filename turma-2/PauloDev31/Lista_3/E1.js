let rs = require('readline-sync');
let numeroDigitado = rs.questionInt("Quantas vezes imprimir BIP BIP? ");
 let contador = 0
 
    while (contador < numeroDigitado ){
        console.log("BIP BIP")
        contador++
    }