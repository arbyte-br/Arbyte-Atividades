let rs = require('readline-sync')
const calcular = require('./calcula-media')

class UsuarioApp {
    constructor(nome, email, endereco, pagamento) {
        this.nome = nome
        this.email = email
        this.endereco = endereco
        this.pagamento
    }
}

let novoUsuario = new UsuarioApp()